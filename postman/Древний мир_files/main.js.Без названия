
function CheckMy()
{	if (submitMag==0)	{submitMag=1; return true;
	}else {alert('Подождите обновления страницы'); return false;}
}

function GoRoo() { if(parent.frames['fr_list'] && startGo==1)
  {  
 
   var ranv=Math.floor( Math.random() * ( 10000 + 1 ) );
	if (parent.frames['fr_send']!=null)
	{ 	
		 if (parent.frames['fr_send'].tXFN==0  || parent.frames['fr_send'].tXFN==1)
		 {
			 //parent.frames['fr_list'].location='/xmir/ul/index.php?rand='+rooms; 
		 }
	}}}
function GoUrl(url)
{ if (parent.frames['main']) {	 if (SubUrl==0) {	 SubUrl=1;  parent.frames['main'].location=url;
	 } else 
		 { alert('Подождите перезагрузки страницы');
		  }
		 } else  {parent.location=url;
		 			}
return false;
}

